# URL del API del backend (con /api al final)
VITE_API_URL=http://localhost:4000/api

# URL raíz del backend, sin /api, usada para mostrar imágenes de /uploads
VITE_SERVER_URL=http://localhost:4000

# URL de la imagen QR de pago que se muestra en el formulario público (opcional).
# Si se deja vacío, se muestra un marcador de posición.
VITE_PAYMENT_QR_URL=
